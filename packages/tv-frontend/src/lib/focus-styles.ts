// Large CTAs (Login flow, Home's primary action) get a bold, unmissable ring.
export const FOCUS_RING_LARGE = 'scale-105 ring-4 ring-ring ring-offset-4 ring-offset-background';

// Compact controls (topbar nav/actions) get a lighter highlight so the ring
// doesn't overpower the small pill-shaped buttons it sits inside.
export const FOCUS_RING_COMPACT =
    'bg-accent text-accent-foreground ring-2 ring-ring ring-offset-2 ring-offset-background';
