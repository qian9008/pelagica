# Pelagica 上游合并与功能完善建议 (UPSTREAM_SUGGESTIONS.md)

本建议书整理了 Pelagica 本地定制版本中已实现的二次开发功能（除“分享管理”外），从用户痛点、功能改进建议以及技术实现的角度进行阐述，旨在为官方上游（Upstream）收录、重构或合并这些功能时提供设计参考与最佳实践指导。

---

## 目录
1. [媒体库文件夹视图与多模式切换](#1-媒体库文件夹视图与多模式切换-folder--list-view)
2. [详情页唤起外部播放器](#2-详情页唤起外部播放器-external-player-launch)
3. [封面图自适应与图片加载降级兜底](#3-封面图自适应与图片加载降级兜底-cover-ratio--fallback)
4. [卡片封面悬停一键播放](#4-卡片封面悬停一键播放-hover-play-button)
5. [客户端手动触发媒体元数据刷新](#5-客户端手动触发媒体元数据刷新-refresh-metadata)
6. [播放器基础交互与控制增强](#6-播放器基础交互与控制增强-player-controls-enhancements)
7. [播放器视频体验与格式支持深度定制](#7-播放器视频体验与格式支持深度定制-advanced-playback-features)
8. [详情页智能返回按钮与封面交互增强](#8-详情页智能返回按钮与封面交互增强-detail-page-navigation)
9. [完整中文本地化翻译合入](#9-完整中文本地化翻译合入-chinese-localization)
10. [登录页 IP 与端口分立输入与智能辅助](#10-登录页-ip-与端口分立输入与智能辅助-split-ip--port-login)
11. [媒体库卡片标题与文件名显示模式切换](#11-媒体库卡片标题与文件名显示模式切换-title-display-mode)
12. [统计同意弹窗防阻塞优化](#12-统计同意弹窗防阻塞优化-stats-consent-modal-fix)

---

## 1. 媒体库文件夹视图与多模式切换 (Folder & List View)

### 用户痛点与背景
- **导航死板**：部分用户采用严格的文件夹结构整理媒体文件，直接平铺展示会导致大库极其混乱。原版在深层文件夹返回时，容易因历史栈管理不当造成死循环。
- **视图单一**：海报墙（Poster）并非适合所有场景。例如，对于文件名很长、包含发布组或版本信息的视频，或者音乐/有声书，纵向海报墙无法展示完整文字，用户难以分辨。

### 给上游的建议
- **增加视图切换记忆**：在媒体库右上角提供“海报网格 (Poster)”、“横版网格 (Backdrop)”和“列表模式 (List)”的切换按钮。
- **设计建议**：
  1. 使用 `localStorage` 将用户的视图偏好持久化（可根据 `LibraryType` 进行独立记忆，例如电影库默认为 Poster，音乐/其它库默认为 List）。
  2. 实现一个轻量级的 `MediaViewSwitcher` 交互组件。
  3. 优化路由管理：当进入子文件夹时，动态将文件夹 ID 推入路由参数（如 `/library/:id?folder=:folderId`），并在后退时准确 pop 栈，避免死循环。

---

## 2. 详情页唤起外部播放器 (External Player Launch)

### 用户痛点与背景
- **编码与硬解限制**：Web 浏览器对高级视频编码（HEVC/H.265、AV1 等）和高级音频格式（DTS-HD、TrueHD 等）支持非常有限，经常被迫在服务端进行昂贵的实时转码。此外，浏览器无法实现诸如 HDR/Dolby Vision 映射、双语字幕渲染等原生渲染器的能力。

### 给上游的建议
- **设计通用的外部协议唤起机制**：
  上游可以通过提供一个可选配置，允许用户在详情页或播放界面“唤起外部播放器”。
- **设计建议**：
  1. **配置层**：在“设置 - 播放”中允许用户启用外部播放器，并配置支持的播放器模板（如 PotPlayer、VLC、IINA、MX Player、Infuse）。
  2. **协议生成**：支持使用通用的 URI Scheme。例如：
     - Windows/Linux PotPlayer: `potplayer://[URL]`
     - VLC: `vlc://[URL]`
     - Android Intent 广播协议。
  3. **交互层**：在原生网页播放按钮旁并列放置一个“外部播放”按钮，或者在播放失败（如不支持格式）时提供“尝试外部播放器”的备用选项。

---

## 3. 封面图自适应与图片加载降级兜底 (Cover Ratio & Fallback)

### 用户痛点与背景
- **比例失调**：Jellyfin 中的海报由于抓取来源不同，比例并不都是标准的 2:3（如部分中国电影、欧美早期海报或同人视频封面）。原版强制使用固定高宽比导致封面严重变形或被裁切。
- **空白尴尬**：部分媒体项目缺失特定的 `Thumb` 或 `Primary` 图像，会导致前端渲染出大块的空白占位或者损坏的图片占位符。

### 给上游的建议
- **动态宽高比与优雅降级组件**：
  上游应该设计一个统一的 `SmartImage` 图片渲染组件，内置 Fallback 和 Ratio 策略。
- **设计建议**：
  1. **动态比例**：图片加载前可以提供一个比例兜底（如 `aspect-ratio: 2/3`），加载完成后通过读取图片的实际物理尺寸（`naturalWidth / naturalHeight`）动态修正 `aspect-ratio`，使图片完整端正地展示。
  2. **降级加载链**：当指定的图片类型（如 `Thumb` 缩略图）加载失败（触犯 `onError`）或为空时，组件自动依次尝试请求 `Backdrop`（横版背景图）或 `Primary`（主海报），最后再退回到系统的默认占位图（Placeholder），实现无缝视觉过渡。

---

## 4. 卡片封面悬停一键播放 (Hover Play Button)

### 用户痛点与背景
- **操作路径过长**：在首页或媒体库中，用户想继续观看某个集数或重温电影时，必须先点击卡片进入详情页，再找到播放按钮点击。对于追剧或短视频用户来说，这一路径有不小的多余开销。

### 给上游的建议
- **桌面端悬停快捷按钮**：
  在主界面的媒体卡片上提供悬停一键播放。
- **设计建议**：
  1. 仅在桌面端（`pointer: fine`）检测 `:hover` 悬停状态。
  2. 悬停时，在卡片中央渐显一个半透明的“播放图标”悬浮按钮。
  3. 用户点击此按钮时直接路由至播放页或调用播放逻辑，点击卡片其它空白部分则照常进入详情页。
  4. 支持在系统设置中提供该快捷交互的开关（如“悬停时显示播放按钮”），满足不同偏好。

---

## 5. 客户端手动触发媒体元数据刷新 (Refresh Metadata)

### 用户痛点与背景
- **刮削纠错繁琐**：当影片元数据或海报识别错误、有更新时，用户只能通过 Jellyfin 服务端的控制台后台进行“刷新元数据”操作。客户端用户无法直接在界面上对某个媒体项目进行强制更新或重新刮削。

### 给上游的建议
- **受控的手动刷新接口**：
  在客户端的详情页或卡片上下文菜单中，向有权限的用户提供“刷新元数据”的操作入口。
- **设计建议**：
  1. **权限校验**：通过 API 检查当前登录用户是否具备对该项目的修改权限。
  2. **API 联动**：点击后直接调用 Jellyfin API 的 `/Items/{Id}/Refresh` 端点（可支持传递 `Recursive=true` 或指定仅刷新缺失数据/覆盖所有数据等参数）。
  3. **状态回馈**：前端按钮应有 loading 状态，接口响应后给出提示（如“已向服务器发送刷新请求，请稍后刷新页面”）。

---

## 6. 播放器基础交互与控制增强 (Player Controls Enhancements)

### 用户痛点与背景
- **控制布局拥挤**：原版将“当前播放时间 / 总时长”与播放/暂停控制按钮挤在一起，不仅挤占了操作空间，也使得进度条的显示精度在窄屏上表现不佳。
- **音量误触**：原版音量控制条常驻或占用过多空间，容易造成误触。
- **缺乏倍速**：目前倍速播放已经是所有视频类播放器（即使是媒体流客户端）的基本标配。
- **移动端横屏缺失**：在手机端通过网页浏览器播放时，进入全屏往往无法自动切换为横屏，需要用户在系统通知栏中手动解锁自动旋转，体验极其割裂。

### 给上游的建议
- **现代播放器面板布局升级**：
  将播放控制面板进行重构以提升可用性。
- **设计建议**：
  1. **时间重排**：参考主流播放器，将时间显示（`01:23 / 02:45`）从按钮旁移至进度条右上方或进度条下方的左右两侧。
  2. **收纳音量条**：音量条默认仅展示静音/音量图标，当鼠标悬停或触摸图标时，再向上或向右滑出滑块。
  3. **内置倍速控制**：新增“播放速度”菜单（支持 0.5x, 0.75x, 1.0x, 1.25x, 1.5x, 1.75x, 2.0x），并在更换集数时通过 `localStorage` 保持当前播放倍速。
  4. **移动端全屏横屏锁**：在进入全屏（`requestFullscreen`）时，调用 `screen.orientation.lock('landscape')`。若浏览器不支持，则通过 CSS 的 `transform: rotate(90deg)` 进行方向旋转适配。
  5. **无缝 duration 预初始化**：播放器初始化时，利用 API 返回的 `RunTimeTicks` 预设进度条最大值，杜绝加载阶段总时长显示为 `0:00` 并伴随闪烁的糟糕视觉。

---

## 7. 播放器视频体验与格式支持深度定制 (Advanced Playback Features)

### 用户痛点与背景
- **转码盲盒**：用户无法直观得知当前播放是否触发了服务端的转码（可能由于带宽限制或格式不支持）。
- **字幕代码残留**：当播放带特效的 SRT 或 ASS 外挂字幕时，因为浏览器渲染引擎不支持字幕的特效指令，经常在画面上留下一堆如 `{\fn微软雅黑\fs18\b1\c&HFFFFFF&}` 这样的未解析标记，极大地干扰阅读。
- **STRM 流重复转码**：`.strm` 文件通常是已经封装好的互联网直播流或网盘直链，再次强行转码容易增加服务器负载且经常播放报错。

### 给上游的建议
- **深度播放体验优化**：
  - **HW/SW 解码状态指示器**：
    利用现代浏览器的 `navigator.mediaCapabilities` API 或者 Jellyfin 后端反馈的播放信息，在播放器右上角悬浮显示一个小型的解码状态标记。当判定是硬解时显示绿色的 `HW`，软解显示橙色的 `SW`，转码显示 `Transcoding`。
  - **特效字幕清洗器**：
    在字幕加载解析为文本轨道前，提供一个全局可选的清洗函数。通过正则表达式 `/{[^}]+}/g` 对文本中的大括号指令进行清理，仅保留纯文本用于浏览器默认渲染。
  - **外挂字幕样式配置面板**：
    允许用户全局设置外挂字幕的样式（如描边大小、颜色、阴影深度、字体、背景透明度），并自动映射为 HTML `<video>::cue` 的 CSS 变量。
  - **STRM 直流识别**：
    在解析播放源时，检测底层源是否为 `.strm`。若是，则默认强制采用 Static 直流播放方式（直接重定向到源地址），跳过转码逻辑。

---

## 8. 智能返回按钮与详情页封面交互增强 (Detail Page Navigation)

### 用户痛点与背景
- **导航链路中断**：如果用户是通过外部链接分享、或者在多标签页中直接点入详情页的，因为没有历史栈，点击返回时无法退出到媒体库。

### 给上游的建议
- **详情页顶层悬浮导航**：
  - **智能返回按钮**：
    在电影、剧集、季、合集、单集详情页的左上角，添加一个悬浮的“返回”按钮。
    **路由逻辑设计**：
    1. 点击时，首先检测是否有浏览器历史记录（`window.history.length > 1`），若有，则执行 `history.back()`。
    2. 若没有（如冷启动进入），则根据当前 Item 类型智能计算上一级路径。例如：单集页面 -> 返回对应的“季页面”；季页面 -> 返回对应的“剧集页面”；电影/剧集页面 -> 返回对应的“媒体库根目录”。
  - **封面图整体点击播放**：
    既然海报封面是详情页最主要的视觉焦点，允许用户直接点击封面触发播放逻辑（相当于点击“播放”按钮），同时当鼠标移动到封面图上时，在封面中央渐显播放图标。

---

## 9. 完整中文本地化翻译合入 (Chinese Localization)

### 用户痛点与背景
- **语言缺失**：作为中文用户基数极大的 Jellyfin 客户端，如果前端界面充斥着英文残余，会显著降低产品的易用度与亲和力。

### 给上游的建议
- **规范化合并 i18n 资源**：
  将目前已翻译完整的 400+ 个条目（覆盖 common, home, item, library, login, player, search, settings, sidebar 等模块）整合进 `frontend/src/locales/zh` 目录，并在主入口注册 `zh` 语言包。

---

## 10. 登录页 IP 与端口分立输入与智能辅助 (Split IP & Port Login)

### 用户痛点与背景
- **输入混淆**：许多非专业用户不理解 `http://` 或者是 IP 地址后面需要带冒号和端口。原版单输入框非常容易因为拼写错误（如漏写冒号、写错协议、带上了尾部斜杠等）导致连接失败。

### 给上游的建议
- **拆分地址输入与自动填充**：
  在登录界面将服务器地址输入框分离为“IP/主机名”与“端口”两个不同的输入框，并带有默认占位符提示。
- **设计建议**：
  1. 拆分后，前端由组件自动进行字符串格式拼装（`http://${host}:${port}` 或 `https://...`）。
  2. **智能辅助**：若客户端是在局域网内运行 of 网页版，开发/运行环境下前端自动读取当前浏览器的 `window.location.hostname` 并预填入 host 输入框，端口预填当前默认端口，极大简化用户的首次配置工作。

---

## 11. 媒体库卡片标题与文件名显示模式切换 (Title Display Mode)

### 用户痛点与背景
- **刮削名称不一致**：部分用户由于刮削信息不够准确，或者其媒体库多为综艺、纪录片、自制剧，其刮削得到的“标题（Title）”非常混乱；反之，其“文件名（FileName）”往往带有清晰的集数和日期。原版强制只显示标题限制了媒体识别的灵活性。

### 给上游的建议
- **显示模式切换配置**：
  允许用户在界面卡片上自主选择是“显示元数据标题”还是“显示原始文件名”。
- **设计建议**：
  1. 编写自定义 Hook（如 `useTitleDisplayMode`）控制这一状态。
  2. 使用 `localStorage` 记忆选择（默认可以为“显示标题”）。
  3. 通过事件监听器（如发布订阅模式或全局 State），当在一处切换显示模式时，当前窗口的所有卡片列表即时同步刷新，以确保交互一致性。

---

## 12. 统计同意弹窗防阻塞优化 (Stats Consent Modal Fix)

### 用户痛点与背景
- **离线或内网阻塞**：有些私有化部署的用户会在局域网或完全与公网隔离的纯内网环境中使用。原版对于收集统计信息的同意弹窗，在服务器接口连接失败时可能会表现为一直等待或禁止关闭，阻塞了用户的核心体验。

### 给上游的建议
- **轻量级免阻塞设计**：
  - 不管接口提交是否成功，用户点击“同意”或“拒绝”之后，弹窗应该立即执行关闭，将状态暂存在本地，后续由后台异步尝试与服务器同步。
  - 允许通过 ESC 键或点击弹窗外区域进行 dismiss，不应强制劫持用户界面。
